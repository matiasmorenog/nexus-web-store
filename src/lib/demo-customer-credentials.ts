/** Credenciales demo del cliente (fuente: prisma/seed-env.ts). Solo servidor / login. */
export {
  SEED_CUSTOMER_EMAIL,
  SEED_CUSTOMER_PASSWORD,
  SEED_CUSTOMER_NAME,
} from "../../prisma/seed-env";
